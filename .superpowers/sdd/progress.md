# CASEONE SDD Progress Ledger
branch: caseone-build
Task 1: complete (scaffold caseone/, commit 2f7a70c) - logged in as shayk@wix.com, token in caseone/seed-artifacts/env.sh
Task 2: complete (stores + forms installed, HTTP 200 both, no files)
Wave 1 dispatched: tasks 3,6,7,9 parallel (agents impl-task3/6/7/9), BASE=2f7a70c, implementers do NOT commit - controller commits after review
Task 3: impl DONE (48 uploads verified, idempotent; orphan dup of abstract-01 in Media, harmless), review pending
Task 6: impl DONE + review pending (form 73c096ee-6b20-4d72-b5cd-959861576426; project_details field not in dashboard UI - platform limitation)
Task 7: impl DONE, review NEEDS FIXES -> fix dispatched to impl-task7: .btn-primary contrast (add --coral-cta >=4.5:1). Deferred minors: desktop-first nav media query, unused font-weight 500, html sizing rule. index.astro prop mismatch resolves at Task 10 (Task 10 MUST pass Layout props).
Task 9: impl DONE (tsc clean; consumers must handle texture loading/error - fold into Tasks 10/11), review pending
Wave 2 dispatched: impl-task4 (CMS seed), impl-task5 (product seed)
Task 3: complete (review Approved; committed). Deferred to final review: transport-level curl failure guard in upload-designs.sh, sizeInBytes as string, stat fallback under set -e. Orphan abstract-01 dup in Media Manager (harmless).
Task 6: review NEEDS FIXES -> fix dispatched to impl-task6 (TEXT_AREA identifier per newer recipe, name-scoped clean, 10s x3 retry). Reviewer's "setup-forms.md doesn't exist" claim was FALSE (exists in scratchpad bundle; implementer followed it) - but the newer node_modules recipe supersedes.
DISCOVERY: caseone/node_modules/@wix/agent-skills/ ships a NEWER wix-headless skill copy with per-vertical astro recipes (e.g. skills/wix-headless/references/astro/forms/CONTACT_FORM.md). Frontend task dispatches (10-14) should consult it alongside the scratchpad bundle; forms frontend detects paragraph via identifier===TEXT_AREA || target===message.
Task 9: review NEEDS FIXES -> fix dispatched to impl-task9 (texture load race token, visible focus ring, aria-label by interactive prop). Deferred minors: redundant double-dispose, pointercancel reset. Reviewer confirmed: dispose coverage complete, SSR-safe, prop contract exact, RoundedBoxGeometry import valid vs three@0.185.0.
Task 4: impl DONE (12/48/8 verified, accent hexes exact, Y2K title fix, idempotent x2) -> review-task4 dispatched
Task 6: fix applied - TEXT_AREA WORKS (4 fields in dashboard summary!), new formId b9ab85e6-fc3d-4b66-8b4f-48a8c551ca39, scoped clean, proper retry -> re-review dispatched. Open note: CONTACT_FORM.md's postSubmissionTriggers.upsertContact contradicts old recipe - untested, deferred (lead-capture works without it; revisit if Task 13 needs contacts auto-creation).
Task 7: complete (re-review Approved, 4.585:1 verified; committed). Deferred minors: desktop-first nav media query, unused font-weight 500, html sizing rule.
Task 9: fix applied (latestRequestedUrl guard, teal focus ring, aria by prop; tsc clean) -> re-review dispatching. Pre-existing unrelated tsc error: astro.config.mjs missing @types/node (scaffold artifact, watch at build gate).
Task 5: impl DONE (productId e70e8dde-caef-4001-a980-f44ebe1ee6ac, 15 variants, prices verified via choiceId resolution, 3 images confirmed via fields=MEDIA_ITEMS_INFO). INTERFACE NOTE: live model choice values are "15","15 Pro","16","16 Pro","16 Pro Max" (no iPhone prefix) - Task 8 constants MUST use these + UI_MODEL_LABELS for display. Re-running seed-store.sh changes productId (deletes+recreates) - never re-run after Task 11 wires constants. -> review-task5 dispatched
Task 8: dispatched to impl-task8 (unblocked by Task 5)
Task 9: complete (re-review Approved; committed). Backlog (final review): designUrl change during initial async scene build silently dropped (narrow first-mount window); redundant double-dispose; pointercancel.
Task 6: complete (re-review Approved; committed). formId b9ab85e6-fc3d-4b66-8b4f-48a8c551ca39. TASK 13 NOTE: paragraph field detection must use field.identifier === "TEXT_AREA" (target is project_details, NOT message). Deferred: postSubmissionTriggers.upsertContact contradiction between recipes (untested).
Task 4: complete (review Approved; committed). Deferred to final review: bulk-insert retry can duplicate (no deterministic IDs - use bulk/items/save for reuse), set -e bypasses retry on transport failures, 200-item query ceiling, live Y2K patch not in script.
Task 8: impl DONE (constants/wix/generateDesigns + test ok; tags split from comma TEXT field) -> review-task8 dispatched
PAGE WAVE dispatched: impl-task10 (homepage), impl-task11 (studio), impl-task12 (gallery+endpoint), impl-task13 (product+about), impl-task14 (cart+badge+checkout). Dev server live on :4322 hot-reloading as pages land. NOT deployed yet - release is Task 15 after page reviews + build gate.
Task 5: complete (review Approved w/ live re-verification; committed). Model-values risk RESOLVED (Task 8 uses live values + UI_MODEL_LABELS). Deferred: name-scoped clean step in seed-store.sh, pagination cap, fold itemsInfo check into script.
Task 8: complete (review Approved, zero Important; committed). Deferred minors: charCodeAt-based FNV (fine, deterministic), ""-vs-null styleSlug inconsistency, query limit caps 50/100 without pagination.
Task 12: impl DONE (endpoint verified live: 400s + one 202; test row "TEST-SUBMISSION-taskl2" unapproved in gallery collection - DELETE BEFORE RELEASE in Task 15) -> review dispatching
Task 14: impl DONE (shapes cross-checked vs .d.ts; /cart SSRs 200). TASK 15 ADDITIONS: create /thank-you page (checkout callback points there, currently 404); manual cart click-through after Studio lands; delete gallery test row TEST-SUBMISSION-taskl2. -> review dispatching
Task 10: impl DONE (homepage live; controller found hero clips under sticky nav + display type too large at desktop - sent to reviewer). NOTE global.css appended by tasks 10 and 14 (sequential appends, no conflict).
Task 12: complete (review Approved, zero Important; committed). Deferred minors: rate limiting on gallery-submit, 12-slug list duplicated in 3 places (consolidate at final pass).
Task 14: review NEEDS FIXES -> fix dispatched (Critical: finally-clear pending in handleRemoveItem - was permanently disabling Checkout after first successful removal; Important: monotonic seq guard vs stale mutation responses). Deferred minors: key fallback, resolveCartImage regex. SDK shapes verified correct vs .d.ts.
Task 13: impl DONE (live product fetch, one live form submission verified, stripHtml fix) -> review-task13 running. No wixMetadata (pre-decided: static routes only).
Task 11: impl DONE -> review-task11 running.
Task 10: review NEEDS FIXES -> fix dispatched (Critical: tagline missing "exactly", stat 2 missing sentence - plan's own line-break example dropped "exactly", constraints govern; Important: hero-scoped display size to fit fold). Nav-clipping was controller false positive (shared browser scroll drift). Minor deferred: aspect-ratio on parent vs img in StylesWall.
NOTE: DevDock browser is SHARED - agents navigating it can corrupt each other's visual checks; measure at hard scrollY:0.
Task 14: complete (re-review Approved; committed cart.astro/Cart.tsx/CartBadge.tsx - global.css held until 10/11 approve). Deferred: key fallback, resolveCartImage regex, retry-vs-inflight-seq theoretical edge.
Task 13: complete (review Approved; committed). Deferred: recipe-inherited server-side field-error mapping mismatch (recipe-level note), static pricing table vs live variants.
Task 11: review NEEDS FIXES -> fix dispatched (Critical: customTextFields need a real product free-text modifier - PATCH live product + seed-store.sh + single "design-notes" key + temp-route verification; Important: clamp ?prompt= to 280, visible notice on metadata fallback). Deferred minor: remove-button aria-label.
Task 10: complete (re-review resolved all findings + pre-approved one-line accent fix verified in file; committed incl. global.css with studio CSS section from task 11 round-1-reviewed). Deferred minor: StylesWall aspect-ratio on parent vs img.
3D realism + UV fix: visually verified (design edge-to-edge, iPhone silhouette) and committed. Conversion pass: verified live (announcement, ratings, ticker) and committed - full section-by-section review folds into FINAL whole-branch review. Reveal sweep fix committed.
FINAL REVIEW must also cover: conversion-pass files, realism-pass delta, ticker grammar ("a Anime" -> "an"), deferred minors list throughout this ledger.
Task 11: round-2 verdict NEEDS FIXES (new finding: variant selection must use options.variantId, not options.options - modifier-only field; sandbox no-op masks it from any test). Round-3 fix dispatched: VARIANT_ID_BY_MODEL_FINISH map from live product + catalogReference change. Round-2 items all verified correct (modifier key casing trap avoided).
POST-RELEASE SMOKE MUST INCLUDE: real add-to-cart on the released site verifying line item carries correct variant (model/finish/price) + Design notes field.
Task 11: COMPLETE (round-3 fix controller-verified: options.options removed, variantId map keys consistent constants<->seed STEP5, 2 spot-checked variantIds confirmed live, both fallback tiers set metadataAttached=false, tsc clean, test-generate ok; committed). Round-3 reviewer died at session limit before verdict - controller performed the focused verification (delta was the reviewer's own prescribed fix). MANDATORY post-release: real add-to-cart smoke (sandbox no-ops all cart calls).
